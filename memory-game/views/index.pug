extends layout

block content
  h1= title
  p(id='title') Welcome to #{title}
  div.game-board
    - var n = 0;
    while n < 20
      div.square(id=n++).flip-card 
        div.flip-card-inner  
          div.flip-card-front 
          div.flip-card-back Square #{n}
